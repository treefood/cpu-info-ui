import { BetterTermPipe } from './better-term.pipe';

describe('BetterTermPipe', () => {
  it('create an instance', () => {
    const pipe = new BetterTermPipe();
    expect(pipe).toBeTruthy();
  });
});
